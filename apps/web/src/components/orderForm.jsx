import { useState, useReducer } from 'react'

export default function OrderForm() {
  cosnt[(slideIndex, setSlideIndex)] = useState(0)
  const [state, dispatch] = useReducer((action, state) => {}, {
    type: '',
    neck: 'round',
    minhag: '',
    size: '',
    address: { format: '', coordinates: [] },
    neverWorn: true
  })
    
    
}
